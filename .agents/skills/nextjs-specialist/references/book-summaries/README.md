# Knowledge notes

Store only durable, non-obvious findings here. Use one file per source section with its source, date, topic tags, a short summary, actionable rules, and links to the pattern it affects. Promote validated guidance into the relevant `patterns/` file; add a concise anti-pattern only when it prevents a recurring failure. Do not duplicate official documentation.
