# Internationalisation

Every localized route needs a self-reference and reciprocal alternate references for the same content set. Point each alternate at its own canonical URL; do not canonicalize one language to another.

Localize titles, descriptions, visible content, structured data, sitemap URLs, and internal links together. Test the rendered HTML of every locale in scope, including fallback and missing-translation behavior.